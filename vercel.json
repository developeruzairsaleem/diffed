{
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install && prisma generate",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
